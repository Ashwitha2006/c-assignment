#include<stdio.h>
main()
{
	int n, i, fact=0;
	printf("enter any number");
	scanf("%d", &n);
    i = 1;
    fact=0;
    while(i<=n)
	{
		if(n%i==0)
		fact++;
	    i++;
	}
	if(fact==2)
	printf("prime %d", n);
	else
	printf("not prime %d", n);
}
